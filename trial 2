#include<stdio.h>
int main()
{
int bursttime[20],process[20],waitingtime[20],turnarndtime[20];
int i,j,n;
printf("Enter the number of processes");
scanf("%d",&n);
printf("Enter the Burst time");
for(i=0;i<n;i++)
{
printf("\nProcess %d:",i+1);
scanf("%d",&bursttime[i]);
process[i]=i+1;
} 
int pos,temp;
for(i=0;i<n;i++)
{
pos=i;
for(j=i+1;j<n;j++)
{
if(bursttime[j]<bursttime[pos])
pos=j;
}
temp=bursttime[i];
bursttime[i]=bursttime[pos];
bursttime[pos]=temp;

temp=process[i];
process[i]=process[pos];
process[pos]=temp;
}
for(i=0;i<n;i++)
{
	printf("\nProcess %d: burst time: %d",process[i],bursttime[i]);

}
return 0;
}
